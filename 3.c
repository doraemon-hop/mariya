program to verify if numbr is positive or not
#include <stdio.h>
void main() 
{
  int num;
  printf("\n enter  number:");
  scanf("\n %d",&num);
  
  if(num>0)
{
    printf("number is positive");
} 
  if(num<0)
  {
      printf("\n number is negetive:");
  }
}
